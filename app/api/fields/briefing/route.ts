import { NextResponse } from "next/server";

/** P3 — generate the AI briefing from computed metric JSON. Stub. */
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}

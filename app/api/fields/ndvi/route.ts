import { NextResponse } from "next/server";

/** P3 — on-demand NDVI fetch/aggregation for a field. Stub. */
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}

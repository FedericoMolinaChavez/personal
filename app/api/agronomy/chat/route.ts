import { NextResponse } from "next/server";

/** P1 — grounded chat over the document library (streaming later). Stub. */
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}

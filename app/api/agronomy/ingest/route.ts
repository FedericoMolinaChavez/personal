import { NextResponse } from "next/server";

/** P1 — ingest an agronomy document (parse + chunk + embed). Stub. */
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}

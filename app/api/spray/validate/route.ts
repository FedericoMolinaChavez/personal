import { NextResponse } from "next/server";

/** P2 — validate a parsed record against rate/wind/REI/PHI rules. Stub. */
export async function POST() {
  return NextResponse.json({ error: "not_implemented" }, { status: 501 });
}

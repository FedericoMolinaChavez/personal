/**
 * P3 — AI briefing built from computed metric JSON (never raw imagery); cites
 * actual numbers only. Stub.
 */
export async function generateBriefing(): Promise<{
  summary: string;
  metrics: unknown;
}> {
  throw new Error("fields.generateBriefing not implemented");
}

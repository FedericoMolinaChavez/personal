/**
 * P3 — fetch recent + forecast weather per field (Open-Meteo/NOAA) into
 * fields.weather_readings. Stub.
 */
export async function refreshWeather(): Promise<void> {
  throw new Error("fields.refreshWeather not implemented");
}

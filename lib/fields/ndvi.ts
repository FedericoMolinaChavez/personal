/**
 * P3 — Field Health Dashboard: fetch + cloud-mask + per-field aggregate NDVI
 * (Sentinel-2), cached into fields.ndvi_readings. Stub.
 */
export async function refreshNdvi(): Promise<void> {
  throw new Error("fields.refreshNdvi not implemented");
}

/**
 * P1 — Agronomy Knowledge Assistant: parse + chunk + embed documents into
 * agronomy.chunks. Stub; may import from lib/shared only (never other tools).
 */
export async function ingestDocument(): Promise<void> {
  throw new Error("agronomy.ingestDocument not implemented");
}
